# Purpose

Convert a Guiguts 1 `.bin` file to a basic Guiguts 2 `.json` file. Only the book
language and page position information are supported at this point.

# Usage

```
perl convert.pl project.txt.bin
```

The converted file will be saved in `project.txt.json`.

